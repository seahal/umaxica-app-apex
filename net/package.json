{
	"name": "umaxica-app-apex-staging-net",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"server": "wrangler dev --ip 0.0.0.0 --port 8784",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.10.4"
	},
	"devDependencies": {
		"wrangler": "^4.4.0"
	}
}
