{
	"name": "umaxica-app-apex-staging-app",
	"type": "module",
	"scripts": {
		"server": "wrangler dev --ip 0.0.0.0 --port 8787",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.10.4"
	},
	"devDependencies": {
		"wrangler": "^4.4.0"
	}
}
